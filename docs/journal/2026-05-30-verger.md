# Journal — 2026-05-30 — Portage direction VERGER (design-first)

**Branche** : `migration/verger` (depuis `origin/main` @ 55e596a, prod avec P0 #27)
**PR** : #29 (`migration/verger → main`) · supersède #28 (hallmark, fermée sans merge)
**Commit** : `76bd4f4`

## Fait

Portage complet de la direction de design **VERGER** (sauge `#6E9551` / abricot `#E89F62` / paper `#FBFAF5`, Bricolage Grotesque + DM Sans) sur tout le produit, à partir du kit `smartfarm-verger-migration`. **Design-first, zéro touche base de données** (Niveau A données + mécanique RPC = hors périmètre).

Stratégie : re-skin **par tokens d'abord** (couche VERGER en fin de `:root` re-câblant `--sf-*` + vars shadcn + `@theme`) → tout l'app re-teinté sans réécrire les écrans. Orchestré en workflows multi-agents (audit → composants → écrans → finissage), fichiers disjoints en parallèle.

- **W0 fondations** : fonts self-hostées (Bricolage 4 poids, DM Sans 3 poids), bascule tokens, radius arrondis, mode plein soleil `data-contrast=high` VERGER, H1 casse naturelle.
- **W1 composants** : `design-verger-shell.css` (sidebar claire/bottom-nav/topbar), `design-verger-catalog.css` (22 familles : `.kpi/.urg/.pn/.tbl/.seg2/.qchips/.bldg/.lot/.ech/.bio/.tl/.bignum/.live` + skeleton/empty/error), reskin button/badge/card/input/select, `states.tsx`. Fix contraste sidebar/drawer (text-white → tokens).
- **W2 écrans** : 47 routes conformées aux 9 gabarits du prototype (présentation only).
- **Finissage** : hydration #18 corrigée (~15 dialogs, trigger rendu client par défaut, pattern `b3a6ada`), FAB unique (#15, suppression des 10 `_fab.tsx`), boutons création (alertes/stock/pesees) rendus visibles tous viewports, cartes plates.

## Résout

- #15 (double FAB superposé) — FAB unique global.
- #18 (hydration React #418 dialogs) — trigger Dialog côté client.

## Validation (CLAUDE.md §4)

- `tsc --noEmit` : 0 erreur
- `build:next-only` : exit 0
- `e2e:smoke` desktop + mobile : 37/37 passed (contre `SMOKE_URL=http://localhost:3200`)
- Scan hydration (15 routes, dont reproduction/stock/bandes/alimentation-matieres ex-cassées) : 0
- Revue adversariale sur la diff : 0 régression logique (fetches Supabase, filtres animaux vivants, P0 #27 préservés)

Diff : 85 fichiers (75 modifiés, 10 ajoutés [3 CSS/states + 7 woff2], 10 `_fab` supprimés), ~−455 lignes nettes.

## Reste

- **Merge #29** = décision Christophe (push sur main = auto-deploy prod). Recette finale prod après merge.
- Hors périmètre VERGER design (plus tard) : Niveau A données du kit (migrations Supabase additives, lib métier testée, RPC), issues #16/#17/#22/#23/#24/#25.
- Polish mineur restant noté par les agents : densités tables mobile, focus champs clavier-only vs tactile, boutons sm 44px — à valider en usage terrain.
